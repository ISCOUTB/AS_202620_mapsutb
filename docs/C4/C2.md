```mermaid
C4Container
    title Diagrama de Contenedores (C4 - Nivel 2) - MAPSUTB

    Person(usuario, "Usuario del campus", "Aspirante, estudiante de intercambio o comunidad UTB")

    Container_Boundary(mapsutb, "MAPSUTB") {
        Container(app, "App movil", "Flutter / Dart", "Interfaz, ruteo aproximado, tour y realidad aumentada. Unico artefacto desplegable")
        ContainerDb(assets, "Activos del mapa", "Archivos empaquetados", "Plano del campus propio: imagen, SVG o grafo de nodos peatonales")
        ContainerDb(datos, "Datos de zonas y puntos de interes", "JSON local", "Clasificacion manual de zonas y coordenadas de puntos de interes")
    }

    System_Ext(arcore, "ARCore Geospatial API", "Servicio externo de Google que ancla objetos de RA a coordenadas GPS reales")

    Rel(usuario, app, "Explora, se ubica y traza rutas", "Pantalla tactil")
    Rel(app, assets, "Lee el plano del campus")
    Rel(app, datos, "Consulta zonas y puntos de interes")
    Rel(app, arcore, "Solicita anclajes geoespaciales", "HTTPS")

    UpdateLayoutConfig($c4ShapeInRow="2", $c4BoundaryInRow="1")

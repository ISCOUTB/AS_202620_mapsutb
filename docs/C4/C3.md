```mermaid
C4Component
    title Diagrama de Componentes (C4 - Nivel 3) - App movil MAPSUTB

    Person(usuario, "Usuario del campus", "Explora, se ubica y traza rutas")

    Container_Boundary(app, "App movil (Flutter)") {
        Component(ui, "Pantallas de features", "Widgets Flutter", "tour, mapas_ruteo, realidad_aumentada, zonas")

        Component(ubicacion, "Servicio de ubicacion", "Observer - Stream<Ubicacion>", "Emite la posicion en tiempo real a quien se suscriba")
        Component(ruteo, "Estrategia de ruteo", "Strategy", "Ruta aproximada: linea recta o grafo peatonal, intercambiables")
        Component(mapaRepo, "MapaRepository", "Repository", "Sirve el plano del campus sin exponer su formato")
        Component(zonaRepo, "ZonaRepository / PuntoInteresRepository", "Repository", "Clasificacion de zonas y puntos de interes")
        Component(arAdapter, "ArCoreAdapter", "Adapter", "Implementa el puerto de anclaje; aisla el SDK de ARCore")
    }

    ContainerDb(assets, "Activos del mapa", "Archivos empaquetados", "Plano propio del campus")
    ContainerDb(datos, "Datos de zonas y puntos de interes", "JSON local", "Clasificacion manual")
    System_Ext(arcore, "ARCore Geospatial API", "Servicio externo de Google")
    System_Ext(gps, "Sensor de ubicacion del dispositivo", "Capacidad de la plataforma")

    Rel(usuario, ui, "Interactua")
    Rel(ui, ubicacion, "Se suscribe al stream")
    Rel(ui, ruteo, "Solicita una ruta aproximada")
    Rel(ui, arAdapter, "Solicita anclajes de RA")
    Rel(ruteo, mapaRepo, "Lee el plano y su conectividad")
    Rel(ui, zonaRepo, "Consulta zonas y puntos de interes")
    Rel(mapaRepo, assets, "Lee")
    Rel(zonaRepo, datos, "Lee")
    Rel(arAdapter, arcore, "Ancla objetos a coordenadas GPS", "HTTPS")
    Rel(ubicacion, gps, "Escucha cambios de posicion")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")

# Uso de IA como herramienta de construcción — MAPSUTB

Este documento registra el uso de IA generativa durante el desarrollo del proyecto: qué se le pidió, qué propuso, qué se aceptó, qué se rechazó y por qué. La IA propone; el equipo decide y verifica.

## Registro de uso

| Fecha | Etapa | Herramienta | Uso / Prompt (resumen) | Resultado | Aceptado / Rechazado | Motivo |
|-------|-------|-------------|--------------------------|-----------|------------------------|--------|
| 07/08/2026 | Declarar | Claude | Exploración de APIs de Google aplicables a un proyecto de mapa universitario con realidad aumentada, y comparación de opciones de arquitectura (frontend/backend) | Listado de APIs (Maps SDK, ARCore Geospatial API, Places, Directions, Geocoding) y sugerencias de stack | Aceptado parcialmente | Se aceptó el listado de APIs como insumo de análisis; la elección final de stack (frontend/backend) queda pendiente de decisión del equipo, no se adoptó automáticamente lo sugerido por la IA. |
| 07/08/2026 | Declarar / Especificar | Claude | Redacción de la ficha del problema y de un escenario de calidad en formato de seis partes para el aspecto A-01 | Borrador de ficha del problema y escenario de calidad | Aceptado con ajustes | El contenido generado fue revisado por el equipo; los umbrales de la medida (3 segundos, 5 metros) quedan sujetos a validación técnica una vez se defina la arquitectura, no son un valor definitivo. |
| 28/08/2026 | Especificar y Verificar | Claude | Revision de las APIs con el nuevo sistema hosteando el mapa de forma local en la app | Se limito las APIs a ARCore SDK base y ARCore Geospatial API | Aceptado | Se reviso por el equipo para evitar que la app este el 100% del tiempo conectada a conexion internet y simplificar el desarrollo de la aplicacion al no usar demasiadas APIs externas |
| 29/08/2026 | Decidir | Claude | Cierre de la decisión final de APIs externas: se descartó realidad aumentada con cámara en vivo (ARCore Geospatial API) y se evaluó reemplazar el mapa base por un plano 100% local vs. superponerlo sobre Google Maps SDK | Se acotaron las dependencias externas a solo dos: Google Maps SDK (mapa base) y Google Geocoding API (geocodificación); se descartaron definitivamente ARCore Geospatial API, Directions API, Places API y Street View | Aceptado | El equipo decidió mantener Maps SDK como mapa base (en vez de un plano completamente propio) para no construir un motor de renderizado de mapas desde cero, y Geocoding API porque no requiere hardware ni permisos adicionales; el ruteo interno se resuelve con Dijkstra sobre el grafo peatonal propio, sin depender de Directions. Esta decisión quedó registrada en el ADR 0001 y propagada al README y a las secciones 3, 4 y 5 de arc42. |
| 30/08/2026 | Verificar | Claude | Revisión de coherencia entre los tres niveles del modelo C4 (C1 Contexto, C2 Contenedores, C3 Componentes) y entre estos y la documentación arc42 (secciones 1 a 5) y el ADR 0001, tras el cambio de alcance que descartó la realidad aumentada | Se identificaron y corrigieron inconsistencias: el actor "Profesores" que no estaba en la ficha de stakeholders, el sensor de ubicación faltante en C1/C2, referencias rotas en el ADR, la meta de calidad de disponibilidad que afirmaba que la app "no depende de internet", y la mención de Directions API como dependencia condicional en las restricciones técnicas | Aceptado con ajustes | El equipo verificó cada corrección propuesta contra el resto de la documentación antes de aplicarla (ningún cambio se aceptó automáticamente); quedan pendientes de revisión `ficha-problema.md`, `aspectos.md` y `escenarios_calidad.md`, que aún describen la versión del proyecto con realidad aumentada y no se han actualizado al alcance actual. |

## Lo que sigue siendo del equipo (no delegado a la IA)

- El alcance exacto del proyecto (outdoor vs. indoor).
- La decisión final de arquitectura y stack tecnológico.
- La verificación de que el código generado (cuando exista) cumple los requisitos y pasa las pruebas.
- La ejecución real de cualquier medición de calidad (no se aceptan medidas propuestas por la IA sin comprobación).

## Notas

- Este archivo se encuentra iniciado como parte de la entrega 1. Se irá completando en cada corte incremental a medida que se use IA en las etapas de Ubicar, Decidir, Construir, Verificar y Evidenciar.
- La entrada del 28/08/2026 quedó redactada antes de la decisión final del 29/08/2026: menciona "ARCore SDK base y ARCore Geospatial API" como las APIs a las que se limitó el proyecto, lo cual ya no es correcto — la decisión final descartó ARCore por completo a favor de Google Maps SDK y Google Geocoding API. Se deja la entrada original sin alterar por trazabilidad histórica del registro, y se aclara la corrección en la entrada del 29/08/2026.
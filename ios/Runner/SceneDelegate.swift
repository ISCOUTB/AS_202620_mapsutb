import Flutter
import UIKit

// Este stub es requerido por `UIApplicationSceneManifest` en Info.plist
// (UISceneDelegateClassName), que activa el ciclo de vida de escenas de
// iOS. `FlutterSceneDelegate` ya implementa todo el comportamiento
// necesario; no se agrega lógica adicional porque la app no usa
// múltiples escenas ni comportamiento específico por escena.
class SceneDelegate: FlutterSceneDelegate {} // NOSONAR: subclase intencionalmente vacía, ver comentario arriba

name: mapsutb
description: >
  Mapa Interactivo de Ubicación con Recorrido 360°
  Esqueleto ejecutable de arquitectura (sin lógica de negocio).
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
